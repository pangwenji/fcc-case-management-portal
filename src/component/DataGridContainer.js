const DataGridContainer = ({ children }) => {
    return (
        <div style={{ height: 450, width: '100%', border: "none" }}>
            {children}
        </div>
    )
}

export default DataGridContainer;